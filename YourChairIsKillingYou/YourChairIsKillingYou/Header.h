#pragma once

int references();
int skynet();
int keywords();
int timer();
int legit(int& terror, int& coffee); //referencing locations with &
int hacked(int& terror, int& coffee); 
int deposit(int *fakeTerrorists, int *fakeBankDeposit); //referencing done within the program, manipulating data with pointers with *